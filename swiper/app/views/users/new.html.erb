<div id="clearance" class="sign-up columns">
  <div class="column is-one-third"></div>
  <div class="column is-one-third">
    <span class="title is-2">Sign Up</span>
    <%= form_for @user do |form|%>
      <%= render partial: '/users/form', object: form %>
    <% end %>
  </div>
</div>
